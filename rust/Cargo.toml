[package]
name = "notepad"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

[dev-dependencies]
criterion = "0.5"
proptest = "1.4"

[[bench]]
name = "benchmarks"
harness = false

[profile.release]
opt-level = 3
lto = true
